<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>{{sala.nome}}</h1>
                <div class="margin mt-2">
                    <a href="/sala" class="btn btn-warning">
                        <i class="fas fa-arrow-left"></i> Voltar
                    </a>
                    <a href="/sala/update/{{sala.id}}" class="btn btn-info">
                        <i class="fas fa-pencil-alt"></i> Alterar
                    </a>
                    <a href="/sala/remove/{{sala.id}}" class="btn btn-danger">
                        <i class="fas fa-times"></i> Remover
                    </a>
                </div>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/">Página Inicial</a></li>
                    <li class="breadcrumb-item"><a href="/sala">Salas</a></li>
                    <li class="breadcrumb-item active">{{sala.nome}}</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<section class="content">
    <div class="row">
        <div class="col">

            <div class="card">
                <div class="card-body table-responsive p-0">


                    <table class="table table-hover">

                        <tr>
                            <td>Nome</td>
                            <td>{{sala.nome}}</td>
                        </tr>

                        <tr>
                            <td>Número da Sala</td>
                            <td>{{sala.numero}}</td>
                        </tr>

                        <tr>
                            <td>Localização da Sala</td>
                            <td>{{sala.localizacao}}</td>
                        </tr>

                    </table>
                </div>
            </div>
        </div>
    </div>
</section>